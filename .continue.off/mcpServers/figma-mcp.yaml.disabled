name: Figma MCP
version: 0.0.1
schema: v1
mcpServers:
  - name: figma
    command: npx
    args:
      - -y
      - "figma-developer-mcp"
    env:
      FIGMA_ACCESS_TOKEN: "${FIGMA_ACCESS_TOKEN}"
    connectionTimeout: 20000