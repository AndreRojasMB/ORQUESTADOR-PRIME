name: Figma MCP
version: 0.0.1
schema: v1
mcpServers:
  - name: figma
    command: npx
    args:
      - -y
      - "figma-developer-mcp"
      - "--figma-api-key"
      - "${FIGMA_API_KEY}"
      - "--stdio"
    connectionTimeout: 20000