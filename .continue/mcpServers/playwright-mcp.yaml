name: Playwright MCP
version: 0.0.1
schema: v1
mcpServers:
  - name: playwright
    command: npx
    args:
      - "@playwright/mcp@latest"
      - "--browser"
      - "chromium"
    connectionTimeout: 30000